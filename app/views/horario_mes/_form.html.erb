<%= form_with(model: horario_me, local: true) do |form| %>
  <% if horario_me.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(horario_me.errors.count, "error") %> prohibited this horario_me from being saved:</h2>

      <ul>
      <% horario_me.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :horario %>
    <%= form.number_field :horario, id: :horario_me_horario %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :horario_me_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
