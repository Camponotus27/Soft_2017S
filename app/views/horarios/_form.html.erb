<%= form_with(model: horario, local: true) do |form| %>
  <% if horario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(horario.errors.count, "error") %> prohibited this horario from being saved:</h2>

      <ul>
      <% horario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comuna %>
    <%= form.text_field :comuna, id: :horario_comuna %>
  </div>

  <div class="field">
    <%= form.label :hora %>
    <%= form.datetime_select :hora, id: :horario_hora %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario, id: :horario_comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
