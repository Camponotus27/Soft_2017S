<p id="notice"><%= notice %></p>

<h1>Horarios</h1>

<table>
  <thead>
    <tr>
      <th>Profesional</th>
      <th>Zona</th>
      <th>Fecha</th>
      <th>Hora</th>
      <th>Disponibilidad</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @horarios.each do |horario| %>
      <tr>
        <td><%= horario.profesional %></td>
        <td><%= horario.zona %></td>
        <td><%= horario.fecha %></td>
        <td><%= horario.hora %></td>
        <td><%= horario.disponibilidad %></td>
        <td><%= horario.descripcion %></td>



                       <% if current_user.nombre == horario.profesional%>
                       <td><%= link_to 'Edit', edit_horario_path(horario) %></td>
                       <td><%= link_to 'Destroy', horario, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                     </tr> <td><%= link_to 'Destroy', horario, method: :delete, data: { confirm: 'Are you sure?' } %></td>



                  <% else %>
                  <% if current_user.permiso == "administrador"%>
                  <td><%= link_to 'Edit', edit_horario_path(horario) %></td>
                  <td><%= link_to 'Destroy', horario, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                </tr> <td><%= link_to 'Destroy', horario, method: :delete, data: { confirm: 'Are you sure?' } %></td>

                <% end %>
                <% end %>


                <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Horario', new_horario_path %>
