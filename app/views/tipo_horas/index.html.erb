<p id="notice"><%= notice %></p>

<h1>Tipo Horas</h1>

<table>
  <thead>
    <tr>
      <th>Descripcion</th>
      <th>Due«Òo hora</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_horas.each do |tipo_hora| %>
      <tr>
        <td><%= tipo_hora.descripcion %></td>
        <td><%= tipo_hora.due«Òo_hora %></td>
        <td><%= link_to 'Show', tipo_hora %></td>
        <td><%= link_to 'Edit', edit_tipo_hora_path(tipo_hora) %></td>
        <td><%= link_to 'Destroy', tipo_hora, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Hora', new_tipo_hora_path %>
