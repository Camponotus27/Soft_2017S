<%= form_with(model: tipo_hora, local: true) do |form| %>
  <% if tipo_hora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_hora.errors.count, "error") %> prohibited this tipo_hora from being saved:</h2>

      <ul>
      <% tipo_hora.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :tipo_hora_descripcion %>
  </div>

  <div class="field">
    <%= form.label :dueÇño_hora %>
    <%= form.text_field :dueÇño_hora, id: :tipo_hora_dueÇño_hora %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
